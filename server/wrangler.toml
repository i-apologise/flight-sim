name = "flight-sim"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# Deploy when ready for Cloudflare Workers Paid multiplayer.
# Local multiplayer uses server-node (see README).

[[durable_objects.bindings]]
name = "GAME_ROOM"
class_name = "GameRoom"

[[durable_objects.bindings]]
name = "LOBBY"
class_name = "Lobby"

[[migrations]]
tag = "v1"
new_classes = ["GameRoom", "Lobby"]
